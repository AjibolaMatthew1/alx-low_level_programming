#include <stdio.h>

/**
 * main - entry point
 * @argc: argument counter;
 * @argv: argument vector
 * Return: 0 (success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	argc = argc;
	return (0);
}
