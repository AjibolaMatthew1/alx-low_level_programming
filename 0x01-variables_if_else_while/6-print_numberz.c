#include <stdio.h>
/*
 * main - Entry point
 * Returns 0 Always(Success)
 */
int main(void)
{
        int i;
        for (i = 0; i <= 9; i++)
        {
                putchar(i);
        }
        putchar('\n');
}
